import 'package:flutter/material.dart';

class FlappyBirdView extends StatelessWidget {
  const FlappyBirdView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}